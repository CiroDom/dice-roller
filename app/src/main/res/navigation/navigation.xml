<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/nav_selec">

    <fragment android:id="@+id/nav_selec"
        android:name="com.example.diceroller.controllers.frags.SelectionFrag"
        tools:layout="@layout/frag_selection">
        <action android:id="@+id/nav_action_selec_to_roll"
            app:destination="@id/nav_roll"/>
    </fragment>

    <fragment android:id="@+id/nav_roll"
        android:name="com.example.diceroller.controllers.frags.RollingFrag"
        tools:layout="@layout/frag_rolling">
    </fragment>

</navigation>